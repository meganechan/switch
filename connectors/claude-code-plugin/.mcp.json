{
  "mcpServers": {
    "switch": {
      "command": "npx",
      "args": ["-y", "@sandbox-quantum/switch-agent-runtime@0.1.0"],
      "env": {
        "SWITCH_API_ENDPOINT": "${SWITCH_API_ENDPOINT}",
        "SWITCH_API_TOKEN": "${SWITCH_API_TOKEN}",
        "SWITCH_AGENT_ID": "${SWITCH_AGENT_ID}",
        "SWITCH_CONNECTION_ID": "${SWITCH_CONNECTION_ID}"
      }
    }
  }
}
